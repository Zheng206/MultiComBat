% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_method.R
\name{stan_algorithm}
\alias{stan_algorithm}
\alias{stan_algorithm.multivariate}
\alias{stan_algorithm.univariate}
\title{Stan algorithm (S3 generic)}
\usage{
stan_algorithm(type, ...)

\method{stan_algorithm}{multivariate}(
  type,
  stan_data,
  chains = 3,
  parallel_chains = 3,
  batch_names,
  ...
)

\method{stan_algorithm}{univariate}(
  type,
  stan_data,
  chains = 3,
  parallel_chains = 3,
  batch_names,
  ...
)
}
\arguments{
\item{type}{S3 tag (e.g., \code{combat_type("univariate")} or \code{"multivariate"}).}

\item{...}{Unused; for S3 compatibility.}

\item{stan_data}{List created by \code{stan_data_prep.univariate()}.}

\item{chains}{Number of MCMC chains.}

\item{parallel_chains}{Number of parallel chains.}

\item{batch_names}{Character vector of batch level names (row names for outputs).}
}
\value{
For multivariate: a list with \code{gamma_star} (named list of \eqn{m \times G} matrices),
\code{delta_star} (named list of length-\eqn{G} lists of \eqn{m \times m} matrices), and mcmc fitted model.

For univariate: a list with matrices \code{gamma_star} and \code{delta_star}
(rows = batches, columns = features).
}
\description{
Stan algorithm (S3 generic)
}
\section{Methods (by class)}{
\itemize{
\item \code{stan_algorithm(multivariate)}: Multivariate Stan routine

\item \code{stan_algorithm(univariate)}: Univariate Stan routine

}}
