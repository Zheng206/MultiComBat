% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eb_method.R
\name{delta_hat_gen}
\alias{delta_hat_gen}
\alias{delta_hat_gen.univariate}
\alias{delta_hat_gen.multivariate}
\title{Compute batch-wise dispersion (variance/covariance) estimates (S3 generic)}
\usage{
delta_hat_gen(type, ...)

\method{delta_hat_gen}{univariate}(type, data_stand_result, batch_result, robust.LS = FALSE, ...)

\method{delta_hat_gen}{multivariate}(
  type,
  data_stand_result,
  batch_result,
  gamma_hat_list,
  robust.LS = FALSE,
  ...
)
}
\arguments{
\item{type}{An object created by \code{\link{combat_type}} or a character scalar.}

\item{...}{Passed to methods.}

\item{data_stand_result}{List of \code{standardize_data()} outputs (one per measurement).}

\item{batch_result}{List of \code{batch_matrix()} outputs (one per measurement).}

\item{robust.LS}{Logical; if \code{TRUE}, use \code{\link{biweight_midvar_mul}}
to form robust \eqn{m \times m} covariances; otherwise use classical covariance.}

\item{gamma_hat_list}{Output of \code{gamm_hat_gen.multivariate()} (used for centering).}
}
\value{
Method-specific structure summarizing \eqn{\delta}
(variances or covariance matrices).

For the univariate method, a matrix \code{delta_hat} of size
(batches \eqn{\times} features) with per-batch feature variances.

For the multivariate method, a named list over batches; each element is
a length-\eqn{G} list of \eqn{m \times m} covariance matrices (one per feature).
}
\description{
Compute batch-wise dispersion (variance/covariance) estimates (S3 generic)
}
