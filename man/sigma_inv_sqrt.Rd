% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/help_func.R
\name{sigma_inv_sqrt}
\alias{sigma_inv_sqrt}
\title{Inverse square root of a (near) SPD matrix via eigen-decomposition}
\usage{
sigma_inv_sqrt(sigma, tol = 1e-06)
}
\arguments{
\item{sigma}{Symmetric (ideally positive semi-definite) \eqn{p \times p} matrix.}

\item{tol}{Numeric tolerance; eigenvalues smaller than \code{tol} are replaced
by \code{tol}. Default \code{1e-6}.}
}
\value{
A \eqn{p \times p} matrix \eqn{\Sigma^{-1/2}}.
}
\description{
Computes \eqn{\Sigma^{-1/2}} from an eigen factorization, thresholding small
or non-positive eigenvalues to \code{tol} for numerical stability.
}
\examples{
set.seed(1)
A <- crossprod(matrix(rnorm(25), 5, 5))  # SPD
S <- sigma_inv_sqrt(A)
round(max(abs(t(S) \%*\% A \%*\% S - diag(5))), 6)

}
\seealso{
\code{\link{robust_matrix_solver}}
}
