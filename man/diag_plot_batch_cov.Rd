% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_diagnostic.R
\name{diag_plot_batch_cov}
\alias{diag_plot_batch_cov}
\title{Plot pooled and batch-specific cross-modality covariance structure}
\usage{
diag_plot_batch_cov(
  data,
  bat,
  covar,
  model = lm,
  formula = NULL,
  ref.batch = NULL,
  use_correlation = TRUE,
  feature_idx = NULL
)
}
\arguments{
\item{data}{A list of length \code{m}, where each element is a matrix of
observations by features for one modality.}

\item{bat}{A list of length \code{m} containing batch labels for each modality.}

\item{covar}{A list of length \code{m} containing covariate data frames.}

\item{model}{A modeling function passed to \code{model_fitting()}. Default is \code{lm}.}

\item{formula}{An optional model formula.}

\item{ref.batch}{An optional reference batch label.}

\item{use_correlation}{Logical; if \code{TRUE} (default), plot correlations.
Otherwise, plot covariances.}

\item{feature_idx}{Integer; if \code{NULL} (default), the covariance structure is
averaged across all features. If a feature index is provided, only that
feature is shown.}
}
\value{
A \code{ggplot2} faceted heatmap showing the pooled covariance or
correlation structure together with one panel per batch.
}
\description{
This diagnostic visualizes the cross-modality covariance or correlation
structure after removing fixed effects and, optionally, batch effects
relative to a reference batch. It displays the pooled structure together
with one heatmap per batch, making it easier to assess heterogeneity in
residual covariance patterns across batches.
}
\details{
Large differences between the pooled panel and the batch-specific panels
may indicate substantial batch-related covariance heterogeneity, in which
case \code{robust_cov = TRUE} may be worth considering.

If \code{feature_idx = NULL}, the function averages covariance structure across
all features. If a feature index is provided, the heatmap is constructed
using only that feature.
}
